import { queries} from './queries/user.query'
import { mutations } from './mutations/user.mutation'
import { resolvers } from './resolvers/user.resolver'
import {types} from './types/user.type'


export const UserQl = {queries, mutations, resolvers, types}
